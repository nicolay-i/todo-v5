## Plan: Анимация модала, статус иконок, поиск по описанию

TL;DR: Ввести универсальный `useModal` для управления открытием/закрытием с анимацией и задержкой размонтирования, применить его к `TodoDescriptionEditor`, усилить визуально кнопку описания при наличии текста (добавить явный класс для SVG), и расширить логику поиска: сохранять диапазоны совпадений описания или флаг и подсвечивать кнопку описания/иконку при match только в `description`.

### Steps
1. Создать `useModal` в `src/lib/hooks/useModal.ts`: состояния `isOpen`, `isMounted`, `open/close`, опциональный `onAfterClose`, управление Escape и фокусом.
2. Добавить CSS/utility анимации (slide-in справа + fade) в `app/globals.css` или Tailwind layer, переиспользовать для description editor и delete confirm.
3. Рефактор `TodoDescriptionEditor.tsx`: заменить локальные open/close на `useModal`, обернуть корень в классы анимации, дождаться `isMounted` перед размонтированием.
4. Расширить поиск: в `src/stores/TodoStore.ts` (функция формирования SearchHighlight) добавить сохранение диапазонов описания (новое поле `descriptionRanges` или флаг `descriptionMatched`), обновить тип в `src/lib/types.ts`.
5. В `TodoStore.getSearchHighlight` (или новый метод) вернуть структуру с дополнительными данными и в `TodoItem.tsx` подсветить кнопку описания (добавить класс/цвет) если есть match только в описании.
6. Усилить визуал иконки описания при наличии текста: добавить класс (например `text-blue-600 stroke-[2.2] ring-2 ring-blue-200`) непосредственно на `<FiFileText />`, убрать неэффективный `font-semibold`.

### Further Considerations
1. Формат хранения описания матчей: отдельные диапазоны vs просто флаг? (Влияет на будущую подсветку внутри редактора.)
2. Анимации: Tailwind plugin или кастомные классы в `globals.css`? (Определить единый стиль.)
3. Нужна ли унификация модала удаления на `useModal` сейчас или отложить?
